<% if user_signed_in? %>
	<% #<h1>Welcome, <%= class: User:name %>
		<% else %>
<div class="hero-unit">
	<h1>Welcome to Captured Clouds!</h1>
	<p>
		You've found the home page for
		<%= link_to "Captured Clouds", root_path, class: "brand" %>
	</p>
	<p>
		<%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
	</p>
</div>
<% end %>